// Where the local mirror of the photo entries lives (git-ignored); see entry-sync.mjs. Shared by the photo
// command and the Admin page's New Photo form, so both work on the same mirror.
export const PHOTO_ENTRIES_DIR = '.photo-entries';
